package {
    import org.axgl.Ax;
    import net.axgl.*;
	
    public class Main extends Ax {
        public function Main():void {
            super(net.axgl.state.GameState);
        }
    }
}